Invoke the plan assistant sub-agent to generate plan.md through an interactive interview.

If plan.example.md exists, use it as a reference example to guide the user.
If plan.md already has content, show it first and ask which parts need revision.

$ARGUMENTS
