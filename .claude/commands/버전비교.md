Invoke the version judge sub-agent to compare all versions in the versions/ directory with a focus on narrative coherence, and select the best combination.
Based on the selection, assemble the final section combination in the drafts/ directory.

$ARGUMENTS
