Invoke the orchestrator sub-agent to autonomously complete the entire business plan.

## Prerequisites
1. project.yaml must exist (if missing, guide the user to /프로젝트설정)
2. plan.md must be sufficiently filled out (item name, target, core features, and team members are required)

## Execution
Delegate the entire process to the orchestrator agent.
Research -> Draft writing (2 versions) -> Devil's advocate attack/reinforcement -> Version comparison -> Integration -> Review loop -> Final version.

## Output
- final/사업계획서.md (Markdown final version)
- final/progress.md (progress log)

hwpx conversion is not included in auto-complete. After confirming the final version, run /한글변환 separately.

$ARGUMENTS
